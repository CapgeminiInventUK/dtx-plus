 const printLine = (message: string) => {
    // TODO Make version dynamics
    // eslint-disable-next-line no-console
    console.log(`%c[DTX Plus v0.0.1 ]%c: ${  message}`,
        "font-size: 14px; color: #88f", "font-size: 14px; color: #fff");
};

export default printLine;

